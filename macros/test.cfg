[gcode_macro test_it]
gcode:


         {% if printer.mmu.flowguard.enabled %}
            MMU_LOG MSG="BLOBIFIER: FlowGuard enabled, adjusting tension"
            MMU_SYNC_FEEDBACK ADJUST_TENSION=1  # adjust tension to ensure we dont accidentaly trigger FlowGuard runout when print resumes
            M400
         {% endif %}
